import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.faeller.zulipnotifs',
  appName: 'Zulip Notifications',
  webDir: 'dist'
};

export default config;
